# Datos/binarios/logs locales fuera del repo. El kit (kit/) SÍ se versiona,
# excepto artefactos residuales.
kit/**/*.db
kit/**/*.log
kit/.env
kit/**/.env

# Eval set: transcritos y resultados son corridas locales, nunca material a
# comitear (pueden contener rutas/datos de la maquina donde se ejecutaron).
kit/evals/transcripts/
kit/evals/resultados-*.json

# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.swp
*~

# Temporales
*.log
*.tmp

# Superpowers SDD scratch (task briefs, diffs, progress ledger)
.superpowers/
